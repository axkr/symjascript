<assembly xmlns="http://maven.apache.org/ASSEMBLY/2.1.1"
          xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
          xsi:schemaLocation="http://maven.apache.org/ASSEMBLY/2.1.1 http://maven.apache.org/xsd/assembly-2.1.1.xsd">
    <!-- Distribution for the GraalVM native image: a single self-contained
         executable, so there is no lib/ and no launcher script - the binary
         needs no Java runtime at all. Built by the 'native' profile. -->
    <id>native</id>
    <formats>
        <format>zip</format>
    </formats>
    <includeBaseDirectory>true</includeBaseDirectory>

    <files>
        <file>
            <source>${project.build.directory}/symjascript</source>
            <outputDirectory>bin</outputDirectory>
            <destName>symjascript</destName>
            <fileMode>0755</fileMode>
        </file>
    </files>

    <fileSets>
        <fileSet>
            <directory>${project.basedir}/src/dist</directory>
            <outputDirectory>/</outputDirectory>
            <includes>
                <include>LICENSE-*.txt</include>
            </includes>
            <fileMode>0644</fileMode>
            <directoryMode>0755</directoryMode>
        </fileSet>
        <fileSet>
            <directory>${project.basedir}/src/dist/native</directory>
            <outputDirectory>/</outputDirectory>
            <includes>
                <include>README.txt</include>
            </includes>
            <fileMode>0644</fileMode>
            <directoryMode>0755</directoryMode>
        </fileSet>
    </fileSets>
</assembly>
